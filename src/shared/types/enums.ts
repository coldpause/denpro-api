export enum PatientType {
  Head = 1,
  Member = 2,
  Other = 3,
}

export enum CreditType {
  Payment = 1,
  Charge = 2,
}

export enum CreditStatus {
  Partial = 2,
  FullyDistributed = 3,
}

export enum UserRole {
  Admin = 'admin',
  Dentist = 'dentist',
  User = 'user',
}
